'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = logFromWantedKeys;

var _lodash = require('lodash.get');

var _lodash2 = _interopRequireDefault(_lodash);

var _constants = require('./constants');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function logFromWantedKeys(_ref) {
  var _ref$event = _ref.event,
      event = _ref$event === undefined ? {} : _ref$event,
      type = _ref.type,
      _ref$keys = _ref.keys,
      keys = _ref$keys === undefined ? [] : _ref$keys,
      log = _ref.log;

  keys.forEach(function (key) {
    var arr = [].concat(key);
    var pathString = arr[0];
    var keyName = arr[1] || arr[0];
    var value = (0, _lodash2.default)(event, pathString);
    log(`${_constants.pluginName}.${type}.${keyName}`, value);
  });
  log(`${_constants.pluginName}.eventType`, type);
}